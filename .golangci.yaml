version: "2"
linters:
  default: all
  disable:
    - revive
    - depguard
    - tagliatelle
    - varnamelen
    - ireturn
    - wsl # deprecated
  exclusions:
    rules:
      - linters:
          - lll
        source: '`[^`]+:"[^"]+"'
